# https://matthewpalmer.net/kubernetes-app-developer/articles/kubernetes-apiversion-definition-guide.html
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot-svc-deployment
spec:
  selector:
    matchLabels:
      app: bot-svc
  replicas: 1
  template:
    metadata:
      labels:
        app: bot-svc
        env: test
    spec:
      containers:
        - name: bot-svc
          image: staffjoy/bot-svc
          imagePullPolicy: Never
          ports:
            - name: http-port
              containerPort: 80
          envFrom:
            - configMapRef:
                name: common-config
---
apiVersion: v1
kind: Service
metadata:
  name: bot-service
spec:
  selector:
    app: bot-svc
    env: test
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80